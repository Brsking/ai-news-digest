# 复制本文件为 .env（或直接在 GitHub Actions Secrets 里配置），不要提交真实值。

# DeepSeek（摘要/翻译用，必填；也可换 OpenAI/通义/混元）
DEEPSEEK_API_KEY=sk-xxxxxxxx

# Resend（默认发信方式，需域名；provider=resend 时必填）
RESEND_API_KEY=re_xxxxxxxx

# 若用 Gmail SMTP 备选（provider=smtp），填这两项：
# SMTP_USER=yourname@gmail.com
# SMTP_PASS=你的16位应用专用密码
